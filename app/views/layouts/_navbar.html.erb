<nav id="header-navbar" class="navbar navbar-inverse navbar-fixed-top navbar-transparent">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <ul class="nav navbar-nav">
        <button class="btn btn-link navbar-btn navbar-theme" onclick="location.href='/';">Home</button>
        <% if current_user %>
          <button id="logged" class="btn btn-link navbar-btn navbar-theme" onclick="location.href='/users/<%= current_user.id %>';"><%= current_user.full_name %>
          </button>
        <% end %>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if current_user %>
          <button class="btn btn-link navbar-btn navbar-theme" onclick="location.href='#';">Logout</button>
        <% else %>
          <button class="btn btn-link navbar-btn navbar-theme" onclick="location.href='#';">Log In</button>
          <button class="btn btn-link navbar-btn navbar-theme" onclick="location.href='#';">Sign Up</button>
        <% end %>
      </ul>
    </div>
  </div>
</nav>